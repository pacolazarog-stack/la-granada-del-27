name: Validate stone matrix

on:
  push:
    paths:
      - 'atlas.html'
      - 'matrix-*.js'
      - 'stone-mesostic.js'
      - 'laboratorio/bajo-la-cal-nucleo.js'
      - 'laboratorio/piedra-matriz.js'
      - 'laboratorio/mesosticos-piedra-redisenados.js'
      - 'laboratorio/validacion-piedra.js'
      - 'validation-loader.mjs'
      - 'validate-stone.mjs'
      - '.github/workflows/validate-stone.yml'
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - run: node validate-stone.mjs
